export const metadata = {
  title: 'Meeting Summaries',
  description: 'Nora`s project',
}

const RootLayout = ({ children }: { children: React.ReactNode }) => (
  <html lang="en">
    <body>{children}</body>
  </html>
)

export default RootLayout
